# Task 5 — Trace the shortest path

Run the path-analysis helper:

```bash
ad-path
```

Submit the complete path using ` -> ` between objects:

```bash
lab-submit path 'OBJECT1 -> OBJECT2 -> OBJECT3 -> OBJECT4 -> OBJECT5'
```

Then click **CHECK**.
