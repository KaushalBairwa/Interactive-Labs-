# Task 7 — Submit the investigation flag

Inspect the validated incident record:

```bash
jq . /root/ad-case/incident.json
```

Submit the flag:

```bash
lab-submit flag 'SWM{YOUR_FLAG}'
```

Then click **CHECK**.
