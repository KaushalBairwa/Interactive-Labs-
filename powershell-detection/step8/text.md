# Task 8 — Submit the incident flag

Run:

```bash
jq . /root/investigation/logs/wazuh-alerts.jsonl
```

Submit the `incident_flag` value:

```bash
lab-submit flag 'SWM{YOUR_FLAG}'
```

Then click **CHECK**.
