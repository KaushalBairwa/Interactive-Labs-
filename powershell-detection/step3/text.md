# Task 3 — Locate the encoded command

Run:

```bash
grep -i "encodedcommand" /root/investigation/logs/sysmon.jsonl
```

Submit the complete Base64 value after `-EncodedCommand`:

```bash
lab-submit encoded 'PASTE_BASE64_VALUE'
```

Then click **CHECK**.
